<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= @company.name %> - Programs</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  <%= javascript_include_tag 'application', 'data-turbo-track': 'reload' %>
</head>
<body>

  <header class="bg-green-600 text-white p-10 flex justify-between items-center">
  <h1 class="text-2xl font-semibold">
    <a href=<%=company_programs_path(@company.slug)%>>
      <%= @company.name %>
    </a>
    <% if @breadcrumbs.present? %>
      <span class="mx-2">/</span>
      <% @breadcrumbs.each_with_index do |breadcrumb, index| %>
        <% if index < @breadcrumbs.length - 1 %>
          <a href="<%= breadcrumb[:path] %>">
            <%= breadcrumb[:name] %>
          </a>
          <span class="mx-2">/</span>
        <% else %>
          <span><%= breadcrumb[:name] %></span>
        <% end %>
      <% end %>
    <% else %>
    <% end %>
  </h1>
  
</header>


  <main>
    <%= yield %>
  </main>

  <footer class="bg-green-600 text-white p-6 mt-10">
    <div class="container mx-auto text-center">
      <p>&copy; <%= Time.current.year %> <%= @company.name %>. All rights reserved.</p>
    </div>
  </footer>
</body>
</html>
